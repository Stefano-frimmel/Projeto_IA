{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Rodar backend FastAPI",
            "type": "shell",
            "command": "cd backend && uvicorn main:app --reload",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
